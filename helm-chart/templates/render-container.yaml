# ---
# apiVersion: v1
# kind: Pod
# metadata:
#   name: render-container
# spec:
#   imagePullSecrets:
#     - name: artifact-registry-secret
#   containers:
#     - name: render-container
#       image: {{.Values.render.image}}
# 			ports:
# 	    - containerPort: {{.Values.render.port}}